#include <unistd.h>
#include <sys/types.h>
#include "libpoc.h"
#include <stdio.h>
#include <stdlib.h>

int main(){
  fork();
  libPocVar++;
  if(libPocVar != 2){
    printf(".so state is shared accross fork");
    exit(1);
  }
  return 0;
}
